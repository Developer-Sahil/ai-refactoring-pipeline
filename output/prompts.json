{
  "file_name": "C:\\dev\\SDP\\input\\order_service.py",
  "language": "python",
  "total_prompts": 9,
  "generated_at": "2026-03-27T09:13:58.541118+00:00",
  "prompts": [
    {
      "chunk_id": "chunk_1",
      "chunk_type": "class",
      "name": "OrderItem",
      "start_line": 17,
      "end_line": 22,
      "prompt": "You are an expert python engineer performing a targeted class-level refactoring.\nRefactor the class `OrderItem` while strictly preserving its public interface and behaviour.\n\nLocation: lines 17–22 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve class and attribute naming to reflect their purpose clearly\n- Add or improve docstrings at the class and method level\n- Simplify complex methods — extract helpers where appropriate\n- Ensure consistent use of access modifiers / visibility conventions\n- Improve readability of the overall class structure\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n- Do NOT alter the class hierarchy or base classes\n\n── Focus Areas ──────────────────────────────────────────────────────────\n- Class-level docstring: describe purpose, key responsibilities, and usage\n- Method-level docstrings: describe what each method does, params, and return value\n- Attribute naming: ensure each attribute name clearly communicates what it holds\n- Method ordering: constructors first, then public, then private/protected\n\n── Original Code ────────────────────────────────────────────────────────\n```python\n@dataclass\nclass OrderItem:\n    \"\"\"A single line-item within an order.\"\"\"\n    product_id: str\n    quantity: int\n    unit_price: float\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored class definition.\nDo not include explanations, markdown prose, or diff output.\nThe returned class must be a drop-in replacement for the original.",
      "original_code": "@dataclass\nclass OrderItem:\n    \"\"\"A single line-item within an order.\"\"\"\n    product_id: str\n    quantity: int\n    unit_price: float"
    },
    {
      "chunk_id": "chunk_2",
      "chunk_type": "class",
      "name": "OrderService",
      "start_line": 25,
      "end_line": 82,
      "prompt": "You are an expert python engineer performing a targeted class-level refactoring.\nRefactor the class `OrderService` while strictly preserving its public interface and behaviour.\n\nLocation: lines 25–82 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve class and attribute naming to reflect their purpose clearly\n- Add or improve docstrings at the class and method level\n- Simplify complex methods — extract helpers where appropriate\n- Ensure consistent use of access modifiers / visibility conventions\n- Improve readability of the overall class structure\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n- Do NOT alter the class hierarchy or base classes\n\n── Focus Areas ──────────────────────────────────────────────────────────\n- Class-level docstring: describe purpose, key responsibilities, and usage\n- Method-level docstrings: describe what each method does, params, and return value\n- Attribute naming: ensure each attribute name clearly communicates what it holds\n- Method ordering: constructors first, then public, then private/protected\n\n── Original Code ────────────────────────────────────────────────────────\n```python\nclass OrderService:\n    \"\"\"Handles order creation, pricing, and lifecycle management.\"\"\"\n\n    def __init__(self, db_conn, tax_rate: float = TAX_RATE):\n        self.db = db_conn\n        self.tax_rate = tax_rate\n        self._cache: dict[str, float] = {}\n\n    def create_order(self, customer_id: str, items: list[OrderItem]) -> dict:\n        \"\"\"\n        Create a new order for *customer_id* containing *items*.\n\n        Returns the persisted order document.\n        \"\"\"\n        subtotal = self._calculate_subtotal(items)\n        tax      = round(subtotal * self.tax_rate, 2)\n        total    = round(subtotal + tax, 2)\n\n        order = {\n            \"customer_id\": customer_id,\n            \"items\":       [vars(i) for i in items],\n            \"subtotal\":    subtotal,\n            \"tax\":         tax,\n            \"total\":       total,\n            \"status\":      \"pending\",\n        }\n        order_id = self.db.insert(\"orders\", order)\n        logger.info(\"Created order %s for customer %s (total: $%.2f)\", order_id, customer_id, total)\n        return {**order, \"order_id\": order_id}\n\n    def _calculate_subtotal(self, items: list[OrderItem]) -> float:\n        \"\"\"Sum the price of all line items.\"\"\"\n        return round(sum(i.quantity * i.unit_price for i in items), 2)\n\n    def cancel_order(self, order_id: str, reason: Optional[str] = None) -> bool:\n        \"\"\"Cancel an existing order.  Returns True on success.\"\"\"\n        order = self.db.find_one(\"orders\", {\"order_id\": order_id})\n        if not order:\n            raise ValueError(f\"Order {order_id} not found\")\n        if order[\"status\"] == \"shipped\":\n            logger.warning(\"Cannot cancel shipped order %s\", order_id)\n            return False\n        self.db.update(\"orders\", {\"order_id\": order_id}, {\"status\": \"cancelled\", \"cancel_reason\": reason})\n        return True\n\n    @staticmethod\n    def format_receipt(order: dict) -> str:\n        \"\"\"Render a plain-text receipt string for *order*.\"\"\"\n        lines = [f\"Order #{order['order_id']}\", \"-\" * 40]\n        for item in order[\"items\"]:\n            lines.append(f\"  {item['product_id']} x{item['quantity']}  ${item['unit_price']:.2f}\")\n        lines += [\n            \"-\" * 40,\n            f\"  Subtotal : ${order['subtotal']:.2f}\",\n            f\"  Tax      : ${order['tax']:.2f}\",\n            f\"  TOTAL    : ${order['total']:.2f}\",\n        ]\n        return \"\\n\".join(lines)\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored class definition.\nDo not include explanations, markdown prose, or diff output.\nThe returned class must be a drop-in replacement for the original.",
      "original_code": "class OrderService:\n    \"\"\"Handles order creation, pricing, and lifecycle management.\"\"\"\n\n    def __init__(self, db_conn, tax_rate: float = TAX_RATE):\n        self.db = db_conn\n        self.tax_rate = tax_rate\n        self._cache: dict[str, float] = {}\n\n    def create_order(self, customer_id: str, items: list[OrderItem]) -> dict:\n        \"\"\"\n        Create a new order for *customer_id* containing *items*.\n\n        Returns the persisted order document.\n        \"\"\"\n        subtotal = self._calculate_subtotal(items)\n        tax      = round(subtotal * self.tax_rate, 2)\n        total    = round(subtotal + tax, 2)\n\n        order = {\n            \"customer_id\": customer_id,\n            \"items\":       [vars(i) for i in items],\n            \"subtotal\":    subtotal,\n            \"tax\":         tax,\n            \"total\":       total,\n            \"status\":      \"pending\",\n        }\n        order_id = self.db.insert(\"orders\", order)\n        logger.info(\"Created order %s for customer %s (total: $%.2f)\", order_id, customer_id, total)\n        return {**order, \"order_id\": order_id}\n\n    def _calculate_subtotal(self, items: list[OrderItem]) -> float:\n        \"\"\"Sum the price of all line items.\"\"\"\n        return round(sum(i.quantity * i.unit_price for i in items), 2)\n\n    def cancel_order(self, order_id: str, reason: Optional[str] = None) -> bool:\n        \"\"\"Cancel an existing order.  Returns True on success.\"\"\"\n        order = self.db.find_one(\"orders\", {\"order_id\": order_id})\n        if not order:\n            raise ValueError(f\"Order {order_id} not found\")\n        if order[\"status\"] == \"shipped\":\n            logger.warning(\"Cannot cancel shipped order %s\", order_id)\n            return False\n        self.db.update(\"orders\", {\"order_id\": order_id}, {\"status\": \"cancelled\", \"cancel_reason\": reason})\n        return True\n\n    @staticmethod\n    def format_receipt(order: dict) -> str:\n        \"\"\"Render a plain-text receipt string for *order*.\"\"\"\n        lines = [f\"Order #{order['order_id']}\", \"-\" * 40]\n        for item in order[\"items\"]:\n            lines.append(f\"  {item['product_id']} x{item['quantity']}  ${item['unit_price']:.2f}\")\n        lines += [\n            \"-\" * 40,\n            f\"  Subtotal : ${order['subtotal']:.2f}\",\n            f\"  Tax      : ${order['tax']:.2f}\",\n            f\"  TOTAL    : ${order['total']:.2f}\",\n        ]\n        return \"\\n\".join(lines)"
    },
    {
      "chunk_id": "chunk_3",
      "chunk_type": "method",
      "name": "__init__",
      "start_line": 28,
      "end_line": 31,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the method `__init__` while strictly preserving its functionality.\n\nContext: This method is defined inside the `OrderService` class.\n\nLocation: lines 28–31 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve method and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns)\n- Simplify conditional logic and reduce nesting where possible\n- Ensure the method has a single, clear responsibility\n- Improve readability without altering the method signature\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n\n── Original Code ────────────────────────────────────────────────────────\n```python\n    def __init__(self, db_conn, tax_rate: float = TAX_RATE):\n        self.db = db_conn\n        self.tax_rate = tax_rate\n        self._cache: dict[str, float] = {}\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.\n\n── Few-Shot Example ─────────────────────────────────────────────────────\nThe following is an example of the quality and style of refactoring expected.\n\nBEFORE:\n```python\ndef proc(self, d):\n    res = []\n    for i in d:\n        if i > 0:\n            if i % 2 == 0:\n                res.append(i * 2)\n    return res\n```\n\nAFTER:\n```python\ndef process_positive_even_numbers(self, data: list[int]) -> list[int]:\n    \"\"\"\n    Return doubled values for every positive even number in *data*.\n\n    Args:\n        data: A list of integers to filter and transform.\n\n    Returns:\n        A new list containing doubled positive even integers.\n    \"\"\"\n    return [\n        value * 2\n        for value in data\n        if value > 0 and value % 2 == 0\n    ]\n```\nNote: Merged nested ifs into a single condition inside a list comprehension, added type hints and docstring, improved all names.",
      "original_code": "    def __init__(self, db_conn, tax_rate: float = TAX_RATE):\n        self.db = db_conn\n        self.tax_rate = tax_rate\n        self._cache: dict[str, float] = {}"
    },
    {
      "chunk_id": "chunk_4",
      "chunk_type": "method",
      "name": "create_order",
      "start_line": 33,
      "end_line": 53,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the method `create_order` while strictly preserving its functionality.\n\nContext: This method is defined inside the `OrderService` class.\n\nLocation: lines 33–53 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve method and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns)\n- Simplify conditional logic and reduce nesting where possible\n- Ensure the method has a single, clear responsibility\n- Improve readability without altering the method signature\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n\n── Original Code ────────────────────────────────────────────────────────\n```python\n    def create_order(self, customer_id: str, items: list[OrderItem]) -> dict:\n        \"\"\"\n        Create a new order for *customer_id* containing *items*.\n\n        Returns the persisted order document.\n        \"\"\"\n        subtotal = self._calculate_subtotal(items)\n        tax      = round(subtotal * self.tax_rate, 2)\n        total    = round(subtotal + tax, 2)\n\n        order = {\n            \"customer_id\": customer_id,\n            \"items\":       [vars(i) for i in items],\n            \"subtotal\":    subtotal,\n            \"tax\":         tax,\n            \"total\":       total,\n            \"status\":      \"pending\",\n        }\n        order_id = self.db.insert(\"orders\", order)\n        logger.info(\"Created order %s for customer %s (total: $%.2f)\", order_id, customer_id, total)\n        return {**order, \"order_id\": order_id}\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.\n\n── Few-Shot Example ─────────────────────────────────────────────────────\nThe following is an example of the quality and style of refactoring expected.\n\nBEFORE:\n```python\ndef proc(self, d):\n    res = []\n    for i in d:\n        if i > 0:\n            if i % 2 == 0:\n                res.append(i * 2)\n    return res\n```\n\nAFTER:\n```python\ndef process_positive_even_numbers(self, data: list[int]) -> list[int]:\n    \"\"\"\n    Return doubled values for every positive even number in *data*.\n\n    Args:\n        data: A list of integers to filter and transform.\n\n    Returns:\n        A new list containing doubled positive even integers.\n    \"\"\"\n    return [\n        value * 2\n        for value in data\n        if value > 0 and value % 2 == 0\n    ]\n```\nNote: Merged nested ifs into a single condition inside a list comprehension, added type hints and docstring, improved all names.",
      "original_code": "    def create_order(self, customer_id: str, items: list[OrderItem]) -> dict:\n        \"\"\"\n        Create a new order for *customer_id* containing *items*.\n\n        Returns the persisted order document.\n        \"\"\"\n        subtotal = self._calculate_subtotal(items)\n        tax      = round(subtotal * self.tax_rate, 2)\n        total    = round(subtotal + tax, 2)\n\n        order = {\n            \"customer_id\": customer_id,\n            \"items\":       [vars(i) for i in items],\n            \"subtotal\":    subtotal,\n            \"tax\":         tax,\n            \"total\":       total,\n            \"status\":      \"pending\",\n        }\n        order_id = self.db.insert(\"orders\", order)\n        logger.info(\"Created order %s for customer %s (total: $%.2f)\", order_id, customer_id, total)\n        return {**order, \"order_id\": order_id}"
    },
    {
      "chunk_id": "chunk_5",
      "chunk_type": "method",
      "name": "_calculate_subtotal",
      "start_line": 55,
      "end_line": 57,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the method `_calculate_subtotal` while strictly preserving its functionality.\n\nContext: This method is defined inside the `OrderService` class.\n\nLocation: lines 55–57 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve method and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns)\n- Simplify conditional logic and reduce nesting where possible\n- Ensure the method has a single, clear responsibility\n- Improve readability without altering the method signature\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n\n── Original Code ────────────────────────────────────────────────────────\n```python\n    def _calculate_subtotal(self, items: list[OrderItem]) -> float:\n        \"\"\"Sum the price of all line items.\"\"\"\n        return round(sum(i.quantity * i.unit_price for i in items), 2)\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.\n\n── Few-Shot Example ─────────────────────────────────────────────────────\nThe following is an example of the quality and style of refactoring expected.\n\nBEFORE:\n```python\ndef proc(self, d):\n    res = []\n    for i in d:\n        if i > 0:\n            if i % 2 == 0:\n                res.append(i * 2)\n    return res\n```\n\nAFTER:\n```python\ndef process_positive_even_numbers(self, data: list[int]) -> list[int]:\n    \"\"\"\n    Return doubled values for every positive even number in *data*.\n\n    Args:\n        data: A list of integers to filter and transform.\n\n    Returns:\n        A new list containing doubled positive even integers.\n    \"\"\"\n    return [\n        value * 2\n        for value in data\n        if value > 0 and value % 2 == 0\n    ]\n```\nNote: Merged nested ifs into a single condition inside a list comprehension, added type hints and docstring, improved all names.",
      "original_code": "    def _calculate_subtotal(self, items: list[OrderItem]) -> float:\n        \"\"\"Sum the price of all line items.\"\"\"\n        return round(sum(i.quantity * i.unit_price for i in items), 2)"
    },
    {
      "chunk_id": "chunk_6",
      "chunk_type": "method",
      "name": "cancel_order",
      "start_line": 59,
      "end_line": 68,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the method `cancel_order` while strictly preserving its functionality.\n\nContext: This method is defined inside the `OrderService` class.\n\nLocation: lines 59–68 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve method and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns)\n- Simplify conditional logic and reduce nesting where possible\n- Ensure the method has a single, clear responsibility\n- Improve readability without altering the method signature\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n\n── Original Code ────────────────────────────────────────────────────────\n```python\n    def cancel_order(self, order_id: str, reason: Optional[str] = None) -> bool:\n        \"\"\"Cancel an existing order.  Returns True on success.\"\"\"\n        order = self.db.find_one(\"orders\", {\"order_id\": order_id})\n        if not order:\n            raise ValueError(f\"Order {order_id} not found\")\n        if order[\"status\"] == \"shipped\":\n            logger.warning(\"Cannot cancel shipped order %s\", order_id)\n            return False\n        self.db.update(\"orders\", {\"order_id\": order_id}, {\"status\": \"cancelled\", \"cancel_reason\": reason})\n        return True\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.\n\n── Few-Shot Example ─────────────────────────────────────────────────────\nThe following is an example of the quality and style of refactoring expected.\n\nBEFORE:\n```python\ndef proc(self, d):\n    res = []\n    for i in d:\n        if i > 0:\n            if i % 2 == 0:\n                res.append(i * 2)\n    return res\n```\n\nAFTER:\n```python\ndef process_positive_even_numbers(self, data: list[int]) -> list[int]:\n    \"\"\"\n    Return doubled values for every positive even number in *data*.\n\n    Args:\n        data: A list of integers to filter and transform.\n\n    Returns:\n        A new list containing doubled positive even integers.\n    \"\"\"\n    return [\n        value * 2\n        for value in data\n        if value > 0 and value % 2 == 0\n    ]\n```\nNote: Merged nested ifs into a single condition inside a list comprehension, added type hints and docstring, improved all names.",
      "original_code": "    def cancel_order(self, order_id: str, reason: Optional[str] = None) -> bool:\n        \"\"\"Cancel an existing order.  Returns True on success.\"\"\"\n        order = self.db.find_one(\"orders\", {\"order_id\": order_id})\n        if not order:\n            raise ValueError(f\"Order {order_id} not found\")\n        if order[\"status\"] == \"shipped\":\n            logger.warning(\"Cannot cancel shipped order %s\", order_id)\n            return False\n        self.db.update(\"orders\", {\"order_id\": order_id}, {\"status\": \"cancelled\", \"cancel_reason\": reason})\n        return True"
    },
    {
      "chunk_id": "chunk_7",
      "chunk_type": "method",
      "name": "format_receipt",
      "start_line": 70,
      "end_line": 82,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the method `format_receipt` while strictly preserving its functionality.\n\nContext: This method is defined inside the `OrderService` class.\n\nLocation: lines 70–82 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve method and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns)\n- Simplify conditional logic and reduce nesting where possible\n- Ensure the method has a single, clear responsibility\n- Improve readability without altering the method signature\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n\n── Original Code ────────────────────────────────────────────────────────\n```python\n    @staticmethod\n    def format_receipt(order: dict) -> str:\n        \"\"\"Render a plain-text receipt string for *order*.\"\"\"\n        lines = [f\"Order #{order['order_id']}\", \"-\" * 40]\n        for item in order[\"items\"]:\n            lines.append(f\"  {item['product_id']} x{item['quantity']}  ${item['unit_price']:.2f}\")\n        lines += [\n            \"-\" * 40,\n            f\"  Subtotal : ${order['subtotal']:.2f}\",\n            f\"  Tax      : ${order['tax']:.2f}\",\n            f\"  TOTAL    : ${order['total']:.2f}\",\n        ]\n        return \"\\n\".join(lines)\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.\n\n── Few-Shot Example ─────────────────────────────────────────────────────\nThe following is an example of the quality and style of refactoring expected.\n\nBEFORE:\n```python\ndef proc(self, d):\n    res = []\n    for i in d:\n        if i > 0:\n            if i % 2 == 0:\n                res.append(i * 2)\n    return res\n```\n\nAFTER:\n```python\ndef process_positive_even_numbers(self, data: list[int]) -> list[int]:\n    \"\"\"\n    Return doubled values for every positive even number in *data*.\n\n    Args:\n        data: A list of integers to filter and transform.\n\n    Returns:\n        A new list containing doubled positive even integers.\n    \"\"\"\n    return [\n        value * 2\n        for value in data\n        if value > 0 and value % 2 == 0\n    ]\n```\nNote: Merged nested ifs into a single condition inside a list comprehension, added type hints and docstring, improved all names.",
      "original_code": "    @staticmethod\n    def format_receipt(order: dict) -> str:\n        \"\"\"Render a plain-text receipt string for *order*.\"\"\"\n        lines = [f\"Order #{order['order_id']}\", \"-\" * 40]\n        for item in order[\"items\"]:\n            lines.append(f\"  {item['product_id']} x{item['quantity']}  ${item['unit_price']:.2f}\")\n        lines += [\n            \"-\" * 40,\n            f\"  Subtotal : ${order['subtotal']:.2f}\",\n            f\"  Tax      : ${order['tax']:.2f}\",\n            f\"  TOTAL    : ${order['total']:.2f}\",\n        ]\n        return \"\\n\".join(lines)"
    },
    {
      "chunk_id": "chunk_8",
      "chunk_type": "function",
      "name": "validate_order_items",
      "start_line": 85,
      "end_line": 99,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the function `validate_order_items` while strictly preserving its functionality.\n\nLocation: lines 85–99 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve function and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns, raises)\n- Simplify conditional logic and reduce nesting where possible\n- Replace magic numbers or strings with named constants\n- Improve readability without altering the function signature\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n\n── Original Code ────────────────────────────────────────────────────────\n```python\ndef validate_order_items(items: list[OrderItem]) -> list[str]:\n    \"\"\"\n    Validate a list of order items.\n\n    Returns a list of validation error messages (empty list = valid).\n    \"\"\"\n    errors: list[str] = []\n    for i, item in enumerate(items):\n        if item.quantity <= 0:\n            errors.append(f\"Item {i}: quantity must be positive\")\n        if item.unit_price < 0:\n            errors.append(f\"Item {i}: unit_price cannot be negative\")\n        if not item.product_id.strip():\n            errors.append(f\"Item {i}: product_id is required\")\n    return errors\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.\n\n── Few-Shot Example ─────────────────────────────────────────────────────\nThe following is an example of the quality and style of refactoring expected.\n\nBEFORE:\n```python\ndef calc(x, n):\n    r = 0\n    for i in range(n):\n        r += x * 1.08\n        x = r\n    return round(r, 2)\n```\n\nAFTER:\n```python\ndef calculate_compound_interest(principal: float, periods: int) -> float:\n    \"\"\"\n    Calculate the total value after applying 8% compound interest.\n\n    Args:\n        principal: The starting amount.\n        periods:   Number of compounding periods.\n\n    Returns:\n        Total value rounded to two decimal places.\n    \"\"\"\n    INTEREST_RATE = 1.08\n    accumulated = principal\n    for _ in range(periods):\n        accumulated *= INTEREST_RATE\n    return round(accumulated, 2)\n```\nNote: Renamed args and loop variable, replaced magic number 1.08 with named constant, added type hints and docstring.",
      "original_code": "def validate_order_items(items: list[OrderItem]) -> list[str]:\n    \"\"\"\n    Validate a list of order items.\n\n    Returns a list of validation error messages (empty list = valid).\n    \"\"\"\n    errors: list[str] = []\n    for i, item in enumerate(items):\n        if item.quantity <= 0:\n            errors.append(f\"Item {i}: quantity must be positive\")\n        if item.unit_price < 0:\n            errors.append(f\"Item {i}: unit_price cannot be negative\")\n        if not item.product_id.strip():\n            errors.append(f\"Item {i}: product_id is required\")\n    return errors"
    },
    {
      "chunk_id": "chunk_9",
      "chunk_type": "async_function",
      "name": "fetch_product_prices",
      "start_line": 102,
      "end_line": 105,
      "prompt": "You are an expert python engineer performing a targeted code refactoring.\nRefactor the async_function `fetch_product_prices` while strictly preserving its functionality.\n\nLocation: lines 102–105 of `C:\\dev\\SDP\\input\\order_service.py`\nLanguage: python\nStyle guide: Follow PEP 8 and PEP 257 conventions.\n\n── Goals ────────────────────────────────────────────────────────────────\n- Improve function and parameter naming to express intent\n- Add or improve the docstring (purpose, args, returns, raises)\n- Ensure async/await usage is idiomatic and error-handling is robust\n- Simplify conditional logic and reduce nesting where possible\n- Replace magic numbers or strings with named constants\n\n── Constraints ──────────────────────────────────────────────────────────\n- Do NOT change the observable behaviour of the code\n- Do NOT rename public APIs, exported symbols, or method signatures\n- Do NOT remove or reorder imports / dependencies\n- Do NOT introduce new external dependencies\n- Do NOT add new logic, features, or side-effects\n- Preserve all existing comments that carry meaningful context\n- Maintain the indentation style of the surrounding file\n- Do NOT change the async/sync nature of the function\n\n── Original Code ────────────────────────────────────────────────────────\n```python\nasync def fetch_product_prices(product_ids: list[str], pricing_api) -> dict[str, float]:\n    \"\"\"Asynchronously fetch current prices for all *product_ids*.\"\"\"\n    results = await pricing_api.batch_get(product_ids)\n    return {pid: results[pid][\"price\"] for pid in product_ids if pid in results}\n```\n\n── Instructions ─────────────────────────────────────────────────────────\nReturn ONLY the refactored code block.\nDo not include explanations, markdown prose, or diff output.\nThe returned code must be a drop-in replacement for the original.",
      "original_code": "async def fetch_product_prices(product_ids: list[str], pricing_api) -> dict[str, float]:\n    \"\"\"Asynchronously fetch current prices for all *product_ids*.\"\"\"\n    results = await pricing_api.batch_get(product_ids)\n    return {pid: results[pid][\"price\"] for pid in product_ids if pid in results}"
    }
  ]
}